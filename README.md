machine codename: poincare

# Notes:
## GNU Stow

### how to stow somewhere other than `$HOME`:
> `sudo stow -t <target_dir> <source_dir>`
> `-t` flag sets the target dir

## md_css:
> source code is intended for use verbatim in the 'Markdown Reader' chromium plugin
